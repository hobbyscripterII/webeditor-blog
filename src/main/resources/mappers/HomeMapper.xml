<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jy.myblog.home.HomeMapper">
    <select id="getPost">
        SELECT a.iboard, a.icategory, a.title, a.contents, b.nm, a.created_at createdAt
        FROM board a
        INNER JOIN user b
        ON a.iuser = b.iuser
        WHERE a.title != ' ' AND a.contents != ' ' AND a.icategory = '1' AND a.public_fl = 'Y'
        ORDER BY a.iboard DESC
        LIMIT 3
    </select>
</mapper>